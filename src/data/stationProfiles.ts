import { StationProfile } from '../types/niagaraService';

// Default station profiles starts empty (0 stations) so every user begins with a clean slate
export const DEFAULT_STATION_PROFILES: StationProfile[] = [];

export const INITIAL_STATION_PROFILES: StationProfile[] = [];
